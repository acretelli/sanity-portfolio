import { useRef, useEffect } from "react"
// import mapboxgl from 'mapbox-gl'
import 'mapbox-gl/dist/mapbox-gl.css'

const Map = () => {
  const NEXT_PUBLIC_MAP_API = ''

  return (
    <div>
      Map
    </div>
  )
}

export default Map